# polars_bridge example

Demonstrates projection patterns for columnar analytics types and numeric operations.

Intended downstream use:

- generated Python types can map naturally into data-frame workflows.
- IR can encode schema semantics for future Arrow/Polars-specific emitters.

