[package]
name = "ferryx-example-async-inference"
version = "0.1.0"
edition = "2024"

[workspace]

[dependencies]
ferryx-macros = { path = "../../crates/ferryx-macros" }
ferryx-runtime = { path = "../../crates/ferryx-runtime" }
tokio = { version = "1", features = ["rt", "macros"] }

