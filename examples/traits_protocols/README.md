# traits_protocols example

Demonstrates trait-oriented design in Rust and projected protocol-style Python typing.

Projection intent:

- trait contracts become Python `Protocol` where semantically safe.
- impl methods remain class-native methods.

