# ferryx Docs

ferryx compiles Rust systems into native Python ecosystems through semantic IR projection.

This documentation set is split between:

- external user guidance (getting started, packaging, verification),
- internal architecture and runtime references.

