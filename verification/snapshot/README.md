# Semantic Snapshot Baselines

Snapshot files are generated from:

Rust source -> IR -> rewrite -> Python emission.

Use:

- `cargo run -p cargo-ferryx -- generate-artifacts ...`
- `cargo run -p cargo-ferryx -- verify-artifacts ...`

