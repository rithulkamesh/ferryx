# Verify Async Projection

Check that async Rust methods map to awaitable Python APIs and retain semantic docs.

Validate:

- `is_async` in IR
- rewrite async attributes
- Python method signatures and behavior notes

